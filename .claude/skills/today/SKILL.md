---
name: today
description: Opens today's journal — receipt, unfiltered entry, the five-second moment, and patterns across past entries. Alias for the journal skill. Use when the user runs /today, says "start my day" or "close out the day".
---

# Today

This is an alias. The daily loop lives in the `journal` skill — invoke it and
follow its steps in full.

Both names exist because the command is `lumis today` but the habit is called
journaling. If the two ever disagree, `journal` is correct.
